# Command line arguments to pass to Java
JAVA_ARGS=

# Configuration file of graylog2-server
CONFIG_FILE="/etc/graylog2.conf"

# Command line arguments to pass to graylog2-server
GRAYLOG2_ARGS="-f ${CONFIG_FILE} --no-pid-file"

# PID file location
PIDFILE="/var/run/graylog2.pid"
